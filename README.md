# Firebase Studio

This is a NextJS starter in Firebase Studio.

To get started, take a look at src/app/page.tsx.

## PHP Version

The `php` directory contains a simplified PHP implementation of the home, about, and contact pages. These examples mirror the basic content of the Next.js site using plain PHP and HTML.
